version: "3"

services:

  lusid-sdk-python-preview-sdk-gen:
    container_name: lusid-sdk-python-preview-sdk-gen
    build: .
    image: finbourne/lusid-sdk-python-preview-sdk-gen
    volumes:
      - .:/usr/swagger
    command: lusid.json